{
  "config": {
    "step": {
      "user": {
        "title": "Spring Input Boolean - Manual Configuration",
        "description": "Select an input_boolean to configure spring behavior for. Found {count} input boolean(s).",
        "data": {
          "entity_id": "Input Boolean Entity"
        }
      },
      "confirm_discovery": {
        "title": "Add Spring Behavior to {name}?", 
        "description": "Do you want to add spring behavior to '{name}' ({entity_id})?\n\nThis will make the input boolean automatically reverse its state after a configurable delay."
      },
      "config": {
        "title": "Configure Spring Behavior for {name}",
        "description": "Configure how '{name}' ({entity_id}) should behave when its state changes.",
        "data": {
          "delay_seconds": "Delay before reversing state (0-60 seconds)",
          "enable_notifications": "Enable notifications when turned off",
          "notification_service": "Notification service to use",
          "phone_entity_ids": "Phone entity IDs (comma-separated, e.g., mobile_app_john_phone, mobile_app_jane_phone)",
          "custom_service_name": "Custom notification service name (e.g., telegram_bot, my_custom_notifier)"
        }
      }
    },
    "error": {
      "custom_service_required": "Custom service name is required when 'Custom notification service' is selected",
      "no_input_booleans": "No input_boolean entities found in your system"
    },
    "abort": {
      "already_configured": "This input_boolean is already configured",
      "no_input_booleans": "No input_boolean entities found"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure Spring Behavior - {entity_id}",
        "description": "Modify the spring behavior settings for this input boolean.",
        "data": {
          "delay_seconds": "Delay before reversing state (0-60 seconds)",
          "enable_notifications": "Enable notifications when turned off",
          "notification_service": "Notification service to use",
          "phone_entity_ids": "Phone entity IDs (comma-separated)",
          "custom_service_name": "Custom notification service name"
        }
      }
    },
    "error": {
      "custom_service_required": "Custom service name is required when 'Custom notification service' is selected"
    }
  }
}
